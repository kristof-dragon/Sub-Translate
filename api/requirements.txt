fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
httpx==0.27.2
python-multipart==0.0.12
pydantic==2.9.2
pytest==8.3.3
# pgsrip wraps tesseract for PGS bitmap subtitle OCR. Pulls babelfish for
# language code parsing and uses imagemagick at runtime (installed via apt
# in the Dockerfile).
pgsrip==0.1.12
