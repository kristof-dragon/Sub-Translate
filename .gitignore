# Persistent runtime data (uploads, translated files, SQLite DB)
data/
*.db
*.db-journal

# Bind-mounted media folder (user MKVs — never commit these)
media/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
*.egg-info/

# Node / React
node_modules/
dist/
.vite/
*.log
*.tsbuildinfo
# tsc -b emit next to sources (vite handles TS directly; we only want the typecheck)
web/src/**/*.js

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local env overrides
.env
